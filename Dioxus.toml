[application]
name = "mechvibes-dx"
out_dir = "bundles"

[web.app]
# HTML title tag content
title = "mechvibes-dx"

# include `assets` in web platform
[web.resource]
# Additional CSS style files
style = []
# Additional JavaScript files
script = []

[web.resource.dev]
# Javascript code file
# serve: [dev-server] only
script = []

# Asset bundling configuration for desktop app
[application.desktop]
# Include assets directory for desktop builds
bundle_assets = true

# bundle configuration
[bundle]
identifier = "com.mechvibesdx"
publisher = "Hai Nguyen"
category = "Utility"
short_description = "A digital mechanical keyboard simulator"
long_description = "This is an application to simulate mechanical keyboard sounds digitally via key detection"
resources = ["./soundpacks"]